. ~/.bash/env

# Clear BASH_ENV so it isn't re-sourced in non-login non-interactive child shells.
# Not exported — intentionally scoped to this login shell only.
BASH_ENV=

if [ "$PS1" ]; then
    . ~/.bash/interactive
fi

