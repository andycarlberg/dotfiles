[tools]
node = "lts"
python = "3"
go = "1"
pnpm = "10"

[settings]
# experimental enables newer mise backends (e.g. aqua, ubi) and in-progress features.
# See: https://mise.jdx.dev/configuration.html#experimental
experimental = true
